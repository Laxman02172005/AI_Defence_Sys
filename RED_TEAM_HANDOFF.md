# RED TEAM HANDOFF: BLUE TEAM INTEGRATION GUIDE

## 1. Overview
This document specifies how the Blue Team (detection and modeling) should consume the synthetic attack corpora generated by the Red Team. The Red Team simulator is now **FROZEN** and outputs a deterministic, stateful, physically consistent dataset of `ACCOUNT_TAKEOVER` (ATO) and `AUTHORIZED_PUSH_PAYMENT` (APP) attack paths.

## 2. Core Data Structure
The Red Team outputs datasets in the form of `AttackRecord` lists (or JSON lines). 

Every `AttackRecord` contains exactly two top-level components relevant to you:
1. `observable_trace`: The physical, observable footprint of the attack as seen by the bank's telemetry.
2. `ground_truth`: The hidden labels representing the attacker's true intent and actions.

### 2.1 The Observable Trace (Features)
**DO USE THIS FOR FEATURE ENGINEERING AND MODEL TRAINING.**
```json
{
  "trace_id": "atk-12345678",
  "events": [
    {
      "envelope": {
        "event_id": "evt-abc",
        "timestamp": "2024-01-01T12:00:00Z",
        "event_type": "SESSION_LOGIN",
        "customer_id": "cust-999"
      },
      "payload": { ... } 
    }
  ]
}
```
*   **Isolation Guarantee:** The `observable_trace` is mathematically guaranteed (and strictly tested) to contain **ZERO** leakage of ground-truth labels. It does not contain the `attack_family`, the `difficulty`, or internal simulator states.
*   **Balance Integrity:** Account balances are intentionally stripped from observable transaction events. The Blue Team must reconstruct balances or features using historical customer baselines, exactly as they would in production.

### 2.2 The Ground Truth (Labels)
**DO USE THIS ONLY FOR LABELING, STRATIFICATION, AND EVALUATION.**
```json
{
  "attack_family": "AUTHORIZED_PUSH_PAYMENT",
  "attack_difficulty": "medium",
  "is_fraud": true,
  "target_amount": 5000.00
}
```
*   You must extract the `attack_family` from this object to train your multi-class models or baseline fraud rates.
*   You must **NOT** pass these fields as inputs into any feature-engineering pipeline.

## 3. Attack Families & Difficulty Semantics

### 3.1 Account Takeover (ATO)
Represents unauthorized access by a third party.
*   **Signatures:** `DEVICE_REGISTRATION` immediately followed by `TRANSACTION`. High transaction fragmentation (many splits). No hesitation gaps.
*   **Difficulty:** Easy = fast/loud. Advanced = Low-and-slow, multi-phase infiltration over days.

### 3.2 Authorized Push Payment (APP)
Represents a legitimate customer being socially engineered.
*   **Signatures:** Uses known, trusted devices and existing sessions. Heavy hesitation gaps (10-120 minutes) between beneficiary addition and payment execution.
*   **Difficulty:** Easy = one-shot fast transfer. Advanced = Heavy friction, large gaps, and multiple failed retries due to bank limits before successful extraction.

## 4. Known Limitations
1.  **APP 'EASY' Diversity:** A basic, one-shot APP scam lacks structural complexity. The Red Team generator correctly refuses to manufacture fake diversity, meaning you will receive fewer mathematically unique `EASY` APP traces compared to other buckets. This reflects reality.
2.  **No Ledger Handoff:** The full physical `WorldState` ledger is intentionally discarded after simulation. The Blue Team must construct entity graphs and historical baselines strictly from the provided `ObservableAttackTrace` events and the Normal World baseline logs.

---
**The Red Team simulation is now formally concluded. Good luck, Blue Team.**
